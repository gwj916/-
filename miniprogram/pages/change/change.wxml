<view class="container" wx:for="{{lanList}}" wx:key="this" data-id="{{index}}" bindtap="selectHanle">
  <view wx:if="{{curLanindex === index}}">
    {{item.chs}}
    <text class="select iconfont icon-duihao"></text>
  </view>
  <view wx:else>
    {{item.chs}}
  </view>
</view>